server:
  port: 8004
spring:
  cloud:
    config:
      server:
         git:
           default-label: config-server
           uri: https://github.com/devhuitzil/Olymicroservice-curso/micros 
           search-paths: config-data
  application:
    name: config-service
